// triangle-side-sum

var points = []
var new_change = function (x, y, point_no) {
  points[point_no] = [x, y]
  createPoint(x, y, function (new_x, new_y, dx, dy) {

  })
}

new_change(.5, .1 ,0)
new_change(.9, .9 ,0)
new_change(.1, .5 ,0)
